# vcftools-ng v0.11.3

Status: **complete for adaptive unfiltered `--counts` fusion and the
1/2/4/8/16/32-thread real-subset gate**.

## Scope

v0.11.3 removes the generic record-object pipeline from the most common
unfiltered `--counts` workload:

- Plain VCF is split at complete-line byte boundaries. Persistent workers use
  independent file descriptors, parse FORMAT/GT directly, and emit final
  VCFtools-compatible count lines.
- BGZF VCF uses direct streaming GT counting at one and two threads. At four
  or more threads, a valid TBI/CSI selects ordered tabix coordinate windows;
  every worker overlaps region decompression, line parsing, GT counting, and
  final formatting.
- A missing BGZF index stays on the fused stream at low thread counts. Above
  four threads, normal automatic-index policy may build CSI and enter the
  indexed fused path. `--no-auto-index` always preserves streaming.
- Ordinary gzip VCF uses the direct streaming parser.
- BCF retains the v0.11.2 adaptive backend because direct VCF text parsing
  does not apply.

The effective worker count is capped by process CPU availability and the
file-descriptor budget. Plain-VCF shard bytes shrink with concurrency so the
active input-buffer target stays near 256 MiB instead of growing as
64 MiB × requested threads.

No new VCFtools parameter was added. The cumulative supported surface remains
the v0.11.2 surface; this release changes execution of eligible `--counts`
commands only.

## Exact compatibility

Oracle: VCFtools 0.1.17.

Fixture: 2,300,000 real records, 100,000 from each of 23 chromosomes, 412
samples. Seven input/index scenarios were run strictly serially. For every
scenario, original ran once and vcftools-ng ran at 1, 2, 4, 8, 16, and 32
threads.

All 42 vcftools-ng outputs passed byte comparison. Every output SHA-256 was:

```text
c2d9a316a7630f092eeec8ab89043f0a5dcc2b0bd579da95b2776bae566731cd
```

The cumulative CTest suite also passed:

- input backend differential: PASS;
- complete supported-surface differential compatibility: PASS;
- adaptive counts at 1/2/4/8/16/32, automatic CSI, no-index policy, unusual
  GT layouts, and polyploid rejection: PASS.

## Performance

Wall-clock seconds from the 32-CPU host:

| Scenario | Original | ng1 | ng2 | ng4 | ng8 | ng16 | ng32 | Speedup range |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| BGZF + TBI | 59.72 | 18.59 | 10.51 | 8.41 | 4.45 | 2.89 | 1.79 | 3.21–33.36× |
| BGZF + automatic CSI | 60.02 | 18.43 | 10.42 | 10.52 | 15.85 | 14.57 | 13.45 | 3.26–5.76× |
| BGZF, no automatic index | 60.18 | 18.50 | 10.46 | 10.44 | 10.40 | 10.40 | 10.42 | 3.25–5.79× |
| Plain VCF | 31.21 | 12.69 | 6.82 | 4.10 | 3.22 | 2.56 | 2.21 | 2.46–14.12× |
| BCF + CSI | 41.56 | 12.64 | 12.61 | 7.16 | 5.04 | 3.22 | 2.49 | 3.29–16.69× |
| BCF + automatic CSI | 41.52 | 12.75 | 12.74 | 14.48 | 8.80 | 5.24 | 4.04 | 2.87–10.28× |
| BCF, no automatic index | 41.46 | 12.68 | 12.58 | 11.25 | 5.78 | 3.37 | 3.36 | 3.27–12.34× |

All 42 candidate measurements are faster than original. Automatic-index
rows include CSI construction from a new path for every measurement. Existing
index rows measure steady-state parallel access.

At 32 threads, CPU utilisation and maximum RSS were:

| Scenario | CPU | Maximum RSS |
|---|---:|---:|
| BGZF + TBI | 2704% | 109,048 KiB |
| BGZF + automatic CSI | 702% | 109,004 KiB |
| BGZF, no automatic index | 205% | 14,504 KiB |
| Plain VCF | 2379% | 311,372 KiB |
| BCF + CSI | 1263% | 726,876 KiB |
| BCF + automatic CSI | 1722% | 705,036 KiB |
| BCF, no automatic index | 950% | 58,788 KiB |

## Evidence and reproduction

- `benchmarks/results/adaptive-v0113-subset-final/summary.tsv`
- `benchmarks/results/adaptive-v0113-subset-final/all-runs.tsv`
- `benchmarks/results/adaptive-v0113-subset-final/manifest.tsv`
- `benchmarks/check-adaptive-regression.sh`
- `benchmarks/run-final-full-matrix.sh`
- `tests/adaptive_counts.sh`

The v0.11.3 routine benchmark intentionally did not run the complete
11,230,392-record dataset. The v0.11.2 five-repeat full-data evidence remains
the most recent full-data compatibility gate.
